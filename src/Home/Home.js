import React, { useState } from 'react';

const PageTitle = () => {
    return (
        <h1>HOME</h1>
    );
};

const returnHome = () => {
    return (
        {PageTitle}
    );
};

export default returnHome;